<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_main.xml — Main Activity Layout
    Host container for fragments with a BottomNavigationView.
    Material Design 3 styling applied via the app theme.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/coordinator_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!-- Fragment Container — Holds the currently active fragment
         (WardrobeFragment, StylistFragment, or AnalyticsFragment) -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="56dp" />

    <!-- Bottom Navigation Bar — Material Design 3 Component
         Provides tab-based navigation between the 3 main modules.
         Uses the menu defined in res/menu/bottom_nav_menu.xml -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="?attr/colorSurface"
        android:paddingTop="0dp"
        android:paddingBottom="0dp"
        app:itemPaddingTop="4dp"
        app:itemPaddingBottom="4dp"
        app:itemIconSize="22dp"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu"
        app:elevation="8dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
